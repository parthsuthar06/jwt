{
  "name": "standalone",
  "version": "1.0.0",
  "description": "jwt signed token using RS256 and SH256 (default) ",
  "main": "jwt.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "parth suthar",
  "license": "ISC",
  "dependencies": {
    "jsonwebtoken": "^8.5.1"
  }
}
